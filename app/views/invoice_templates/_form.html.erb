<%= form_for(@invoice_template) do |f| %>
  <% if @invoice_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invoice_template.errors.count, "error") %> prohibited this invoice_template from being saved:</h2>

      <ul>
      <% @invoice_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
