<%= form_for(@invoice_payment) do |f| %>
  <% if @invoice_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invoice_payment.errors.count, "error") %> prohibited this invoice_payment from being saved:</h2>

      <ul>
      <% @invoice_payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :invoice_id %><br />
    <%= f.number_field :invoice_id %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
