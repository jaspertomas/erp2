<%= form_for(@purchase_template) do |f| %>
  <% if @purchase_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_template.errors.count, "error") %> prohibited this purchase_template from being saved:</h2>

      <ul>
      <% @purchase_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
