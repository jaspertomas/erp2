<%= form_for(@purchase_payment) do |f| %>
  <% if @purchase_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_payment.errors.count, "error") %> prohibited this purchase_payment from being saved:</h2>

      <ul>
      <% @purchase_payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :purchase_id %><br />
    <%= f.number_field :purchase_id %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
